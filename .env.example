# Threads 자동발행용 — 실제 값은 .env(gitignore)에 넣으세요. 이 파일은 템플릿(커밋됨).
# 단기토큰을 처음 한 번 넣고 `python threads_publisher.py` 실행하면 장기(60일)로 교환됩니다.
THREADS_ACCESS_TOKEN=여기에_단기_액세스_토큰
THREADS_USER_ID=37096544403277101
THREADS_APP_SECRET=여기에_앱_시크릿
